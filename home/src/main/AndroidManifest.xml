<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.droidcon.home">

    <dist:module
        dist:instant="true"
        dist:title="@string/title_home">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>
        <dist:fusing dist:include="false" />
    </dist:module>

    <application>
        <activity android:name=".DroidconMadridActivity">
        </activity>
        <activity android:name=".FullExperienceHomeActivity" />
        <activity android:name=".InstantExperienceHomeActivity" />
    </application>
</manifest>

